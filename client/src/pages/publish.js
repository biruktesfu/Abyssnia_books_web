import React from "react";

const Publish = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "Left",
        alignItems: "Left",
        height: "100vh",
      }}
    >
      <h1>Publish</h1>
    </div>
  );
};

export default Publish;
